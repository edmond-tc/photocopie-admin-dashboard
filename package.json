{
  "name": "photocopie-admin-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute photocopie-admin-db --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute photocopie-admin-db --remote --file=./migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "cloudflare": {
    "bindings": {
      "ADMIN_PASSWORD": {
        "description": "Le mot de passe que tu veux utiliser pour te connecter à ton tableau de bord."
      },
      "LICENSE_SECRET": {
        "description": "Copie exacte de la constante SECRET dans src-tauri/src/license.rs — doit être identique, sinon les clés générées ici ne marcheront pas dans l'application."
      }
    }
  }
}
